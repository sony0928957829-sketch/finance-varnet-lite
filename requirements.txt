# Core numerics / IO
pandas>=2.0,<4.0
numpy>=1.24,<3.0
pyyaml>=6.0
scipy>=1.10,<2.0
PyWavelets>=1.4,<2.0
pyarrow>=15.0

# Market data
# Upper bound: yfinance ships breaking changes in minor releases and CI installs
# the newest wheel on every run, so an unbounded pin makes the daily job depend
# on an upstream release we never reviewed.
yfinance>=0.2,<2.0
# REQUIRED by yfinance's price-repair path (repair=True ->
# yfinance/scrapers/history.py: "from sklearn.cluster import DBSCAN").
# scikit-learn is NOT declared as a yfinance dependency, so without this line
# every single download raises ModuleNotFoundError("No module named 'sklearn'")
# and the whole pipeline sees an empty market. This is what broke the daily job.
scikit-learn>=1.3,<2.0

# Storage (Google Drive data lake)
google-api-python-client>=2.0
google-auth>=2.0
google-auth-oauthlib>=1.0

# Tests
pytest>=8.0
